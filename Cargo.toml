[package]
name = "pyhone"
version = "0.1.0"
edition = "2021"

[dependencies]
rustpython-parser = "0.4"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"

[dev-dependencies]
tempfile = "3"
